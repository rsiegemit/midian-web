# Exact versions of the environment that produced every reported number (Python 3.12.11).
# `pip install -e ".[all]"` resolves compatible versions; `pip install -r requirements.txt -e .` reproduces these.
# torch was the CUDA 12.9 build (2.11.0+cu129); install the wheel that matches your platform.
numpy==2.3.5
pandas==3.0.5
scipy==1.18.1
PyYAML==6.0.3
torch==2.11.0
sentence-transformers==6.0.1
scikit-learn==1.9.0
hnswlib==0.8.0
trueskill==0.4.5
openai==3.7.0
reasoning_gym==0.1.25
requests==2.34.2
huggingface_hub==1.29.0
matplotlib==3.11.1
pytest==9.1.1
