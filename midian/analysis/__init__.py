"""Result analysis, split by concern: load (rows, labels, groups), stats (paired statistics), legacy_figures
(diagnostic F1-F7), legacy_targets (pre-registered expectations), report (summary.md and the CLI).
`midian.analyze` re-exports every name, and `python -m midian.analyze --grid G` is the entry point."""
