[project]
name = "rte"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy>=1.26", "pyyaml", "pandas", "scipy"]
[tool.setuptools.packages.find]
include = ["rte*"]
[tool.pytest.ini_options]
testpaths = ["tests"]
